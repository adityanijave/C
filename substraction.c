#include <stdio.h>

void main(){
    // declearing datatype of variables
    int num1, num2, sub;
    // assigning value to variable
    num1 = 10; num2 = 9;
    // substraction operation
    sub = num1 - num2;
    // printing result
    printf("The nunmber %d - number %d is %d", num1, num2, sub);
}